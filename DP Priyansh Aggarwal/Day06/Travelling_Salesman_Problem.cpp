#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef vector<double> vd;
typedef vector<vector<int>> vvi;
typedef vector<vector<double>> vvd;

int main(){
    // Given a set of cities and the distance between every 
    // pair of cities, the problem is to find the shortest 
    // possible route that visits every city exactly once and 
    // returns to the starting point.
    return 0;
}